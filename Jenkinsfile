@Library("/lib")

// This shows a simple build wrapper example, using the AnsiColor plugin.
node {
    stage("run") {
        var foo = new com.markvr.Foo()
        println("Say: " + foo.bar())
    }
}